# Execution Packet

- task_id:
- packet_id:
- owner: executor
- goal:

- allowed_scope:
  - (add scope)

- touched_files:
  - (add file)

- validation:
  - (add validation) (add validation)

- constraints:
  - (add constraint) (add constraint)

- fallback_if_blocked:
  - (add fallback)
